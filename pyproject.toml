[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "suzent"
version = "0.1.0"
description = "Your personal Agent"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "crawl4ai>=0.7.3",
    "smolagents[gradio,litellm,mcp]",
    "starlette",
    "uvicorn",
    "python-dotenv",
    "loguru>=0.7.0",
    "pydantic>=2.0",
    "markitdown[all]",
]


