{
  "name": "suzent-desktop",
  "version": "0.1.2",
  "description": "SUZENT - Your Personal Agent (Desktop Application)",
  "private": true,
  "scripts": {
    "tauri": "tauri",
    "dev": "tauri dev",
    "build": "tauri build --config tauri.conf.prod.json",
    "build:frontend": "cd ../frontend && npm install && npm run build",
    "build:backend": "python ../scripts/build_backend.py",
    "build:full": "npm run build:frontend && npm run build:backend && npm run build"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.0.0-rc.18"
  }
}
