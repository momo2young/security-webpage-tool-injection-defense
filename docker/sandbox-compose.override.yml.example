# Example override file for local configuration
# Rename this to sandbox-compose.override.yml to use it
services:
  microsandbox:
    volumes:
      # Add your local volume mounts here
      # - /path/to/local/data:/mnt/data
